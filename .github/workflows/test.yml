name: 测试

on: push

env:
  hello: campfire
  include: am

jobs:
  demo:
    runs-on: ubuntu-latest
    name: 测试
    steps:
      - name: 打印分支
        if: contains(env.hello, env.include)
        run: echo ${{ github.ref }} - ${{ env.hello }}
